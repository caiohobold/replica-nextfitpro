import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    marginVertical: 10,
  },
  scene: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 20,
  },
});

  export default styles;